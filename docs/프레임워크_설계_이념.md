# CATUI Mobile 프레임워크 설계 이념

## 📋 개요

CATUI Mobile은 IMCAT UI 코어를 기반으로 한 **모바일 전문 웹 프레임워크**입니다.
터치 이벤트, 제스처 인식, 뷰포트 관리 등 모바일 환경에 특화된 기능을 제공합니다.

---

## 🎯 핵심 설계 원칙

### 1. **모바일 우선 (Mobile First)**

모든 기능과 스타일은 모바일 환경을 최우선으로 설계됩니다.

- 터치 인터랙션 최적화
- 44px 이상의 터치 영역
- 엄지손가락 접근성 고려
- 모바일 퍼포먼스 최적화

### 2. **터치 네이티브 (Touch Native)**

```javascript
// 직관적인 터치 API
const touch = CATUI.touch('#element');
touch.on('swipeleft', () => navigateNext());
touch.on('swiperight', () => navigatePrev());
touch.on('longpress', () => showContextMenu());
touch.on('pinch', ({ scale }) => zoom(scale));
```

**지원 제스처:**

- **탭**: tap, doubletap
- **프레스**: longpress
- **스와이프**: swipe, swipeleft, swiperight, swipeup, swipedown
- **핀치**: pinch (확대/축소)
- **회전**: rotate
- **드래그**: dragstart, drag, dragend
- **패닝**: pan

### 3. **뷰포트 적응형 (Viewport Adaptive)**

모바일 특유의 뷰포트 문제를 자동으로 해결합니다.

```javascript
// 100vh 문제 자동 해결
// CSS에서 var(--vh) 사용
.full-height {
  height: calc(var(--vh, 1vh) * 100);
}

// 노치/세이프 에어리어 자동 대응
.header {
  padding-top: var(--sat);
}
.footer {
  padding-bottom: var(--sab);
}
```

### 4. **IMCAT UI 호환성 유지**

기존 IMCAT UI의 모든 코어 기능을 그대로 사용할 수 있습니다.

```javascript
// DOM 조작
CATUI('#app').addClass('active');

// 모듈 로드
const Modal = await CATUI.use('overlays');

// SPA 라우팅
<a catui-href="views/home.html">홈</a>

// 이벤트 버스
CATUI.on('event', handler);
CATUI.emit('event', data);
```

### 5. **PWA 최적화 (PWA Ready)**

Progressive Web App 환경에 최적화되어 있습니다.

```javascript
// PWA 모드 감지
if (CATUI.device.isPWA) {
  // PWA 전용 UI 표시
}

// 네트워크 상태 감지
const network = CATUI.device.networkInfo;
if (network.type === '4g') {
  // 고화질 리소스 로드
}
```

---

## 🏗️ 아키텍처 구조

### 레이어 구조

```text
┌──────────────────────────────────────────────┐
│      Application Layer                       │  ← 사용자 코드
├──────────────────────────────────────────────┤
│      Module Layer (동적)                      │  ← 선택적 모듈
│  Theme, Overlays, BottomSheet, etc.          │
├──────────────────────────────────────────────┤
│      Mobile Core Layer                       │  ← 모바일 전용
│  Touch, Gesture, Viewport, Device, Keyboard  │
├──────────────────────────────────────────────┤
│      IMCAT Core Layer (항상 로드)             │  ← 공통 기반
│  DOM, Event, Loader, Router, Security, etc.  │
└──────────────────────────────────────────────┘
```

### 모바일 전용 모듈

| 모듈 | 설명 |
|------|------|
| `TouchManager` | 터치 이벤트 및 기본 제스처 |
| `GestureRecognizer` | 복합 제스처 (회전, 드래그) |
| `PullToRefresh` | 당겨서 새로고침 |
| `ViewportManager` | 뷰포트 및 방향 관리 |
| `DeviceDetector` | 디바이스 정보 감지 |
| `KeyboardManager` | 가상 키보드 감지 |

---

## 📱 모바일 최적화 기능

### 1. 터치 이벤트 시스템

```javascript
const touch = CATUI.touch('#swipeable', {
  swipeThreshold: 50,      // 스와이프 인식 거리 (px)
  swipeVelocity: 0.3,      // 스와이프 인식 속도
  tapTimeout: 200,         // 탭 인식 시간 (ms)
  longPressTimeout: 500,   // 롱프레스 인식 시간 (ms)
  doubleTapTimeout: 300,   // 더블탭 인식 시간 (ms)
  pinchThreshold: 0.1,     // 핀치 인식 스케일 변화
  preventScroll: false     // 스크롤 방지 여부
});

// 이벤트 등록
touch.on('swipeleft', (data) => {
  console.log('방향:', data.direction);
  console.log('속도:', data.velocity);
});

// 정리 (메모리 누수 방지)
touch.destroy();
```

### 2. 뷰포트 관리

```javascript
// 뷰포트 정보
console.log(CATUI.viewport.width);
console.log(CATUI.viewport.height);
console.log(CATUI.viewport.orientation);

// 세이프 에어리어
const insets = CATUI.viewport.safeAreaInsets;
console.log('노치 높이:', insets.top);

// 이벤트
CATUI.viewport.on('orientationchange', (data) => {
  console.log('방향 변경:', data.orientation);
});
```

### 3. 디바이스 감지

```javascript
const device = CATUI.device;

// 플랫폼 감지
device.isIOS        // iOS 디바이스
device.isAndroid    // Android 디바이스
device.isMobile     // 모바일 디바이스
device.isTablet     // 태블릿
device.hasTouch     // 터치 지원

// 환경 감지
device.isPWA                  // PWA 모드
device.prefersDarkMode        // 다크모드 선호
device.prefersReducedMotion   // 모션 감소 선호
device.pixelRatio             // 화면 밀도

// 네트워크
device.networkInfo.type       // 2g, 3g, 4g, 5g, wifi
device.networkInfo.saveData   // 데이터 세이버 모드
```

### 4. 가상 키보드 관리

```javascript
CATUI.keyboard.on('show', (data) => {
  // 키보드 표시됨
  // 입력 필드가 가려지지 않도록 스크롤 조정
  document.getElementById('input').scrollIntoView();
});

CATUI.keyboard.on('hide', () => {
  // 키보드 숨겨짐
});
```

### 5. Pull-to-Refresh

```javascript
const ptr = CATUI.pullToRefresh('#list', {
  threshold: 80,           // 트리거 거리
  resistance: 2.5,         // 당김 저항
  refreshTimeout: 2000,    // 최대 새로고침 시간
  onRefresh: async () => {
    // 데이터 새로고침
    await fetchNewData();
  }
});

// 메모리 관리
CATUI.view.registerInstance(ptr);
```

---

## 🔒 보안 (IMCAT UI 계승)

모든 IMCAT UI 보안 기능을 그대로 상속합니다.

- XSS 자동 방어
- HTML 새니타이징
- 경로 순회 공격 방지
- CSP 지원

```javascript
// 안전한 텍스트 출력
CATUI('#output').text(userInput);

// 안전한 HTML 출력
CATUI('#content').html(apiResponse);

// 경로 검증
if (CATUI.validatePath(path)) {
  CATUI.view.navigate(path);
}
```

---

## 📐 스타일 시스템

### CSS 변수

```css
:root {
  /* 모바일 전용 변수 */
  --vh: 1vh;                          /* 실제 뷰포트 높이 */
  --sat: env(safe-area-inset-top);    /* 세이프 에어리어 */
  --sar: env(safe-area-inset-right);
  --sab: env(safe-area-inset-bottom);
  --sal: env(safe-area-inset-left);
  --touch-target-min: 44px;           /* 최소 터치 영역 */
}
```

### 유틸리티 클래스

```html
<!-- 풀 하이트 (100vh 문제 해결) -->
<div class="full-height">...</div>

<!-- 세이프 에어리어 패딩 -->
<header class="safe-area-top">...</header>
<footer class="safe-area-bottom">...</footer>

<!-- 터치 타겟 -->
<button class="touch-target">버튼</button>

<!-- 터치 피드백 -->
<div class="touch-feedback">탭하세요</div>

<!-- 스크롤 -->
<div class="scroll-y no-scrollbar">...</div>
```

### SCSS 믹스인

```scss
@use 'abstracts/mixins' as *;

.my-component {
  @include touch-target(48px);
  @include safe-area-bottom;
  @include full-height;
  @include momentum-scroll;
  @include no-tap-highlight;
}
```

---

## ⚡ 성능 최적화

### 1. 터치 이벤트 최적화

- Passive 이벤트 리스너 기본 사용
- 불필요한 이벤트 전파 차단
- 디바운스/스로틀 적용

### 2. 렌더링 최적화

- GPU 가속 트랜스폼 사용
- will-change 적절히 활용
- 리플로우 최소화

### 3. 메모리 관리

- destroy() 패턴 필수 구현
- 인스턴스 자동 정리
- 이벤트 리스너 추적

---

## 🆚 IMCAT UI vs CATUI Mobile

| 특성 | IMCAT UI | CATUI Mobile |
|------|----------|--------------|
| **대상** | 웹 전반 | 모바일 전문 |
| **터치** | 기본 지원 | 고급 제스처 |
| **뷰포트** | 표준 | 노치/키보드 대응 |
| **PWA** | 선택적 | 최적화 |
| **코어** | 동일 | 모바일 확장 |
| **스타일** | 범용 | 모바일 우선 |
| **용량** | ~15KB | ~17KB |

---

## 📁 프로젝트 구조

```text
catui-mobile/
├── src/
│   ├── core/
│   │   ├── index.js        # 메인 진입점 (모바일 확장)
│   │   ├── touch.js        # 터치/제스처 시스템 ★
│   │   ├── viewport.js     # 뷰포트/디바이스 관리 ★
│   │   ├── dom.js          # DOM 유틸리티 (IMCAT)
│   │   ├── router.js       # SPA 라우터 (IMCAT)
│   │   ├── security.js     # 보안 (IMCAT)
│   │   └── ...             # 기타 코어 모듈 (IMCAT)
│   ├── modules/            # 확장 모듈
│   └── styles/             # 모바일 최적화 SCSS
├── dist/                   # 빌드 결과물
├── examples/               # 예제
└── docs/                   # 문서
```

---

## 🚀 시작하기

### 설치

```bash
# npm
npm install @imcat-ckim/catui-mobile

# 또는 직접 다운로드
```

### 기본 사용

```html
<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
  <link rel="stylesheet" href="dist/catui-mobile.css">
</head>
<body>
  <div id="app" class="full-height safe-area-inset">
    <header class="safe-area-top">헤더</header>
    <main class="scroll-y">콘텐츠</main>
    <footer class="safe-area-bottom">푸터</footer>
  </div>
  
  <script type="module">
    import CATUI from './dist/catui-mobile.js';
    
    // 터치 이벤트
    const touch = CATUI.touch('#app');
    touch.on('swipeleft', () => console.log('스와이프!'));
    
    // 디바이스 정보
    console.log('모바일:', CATUI.device.isMobile);
    console.log('PWA:', CATUI.device.isPWA);
  </script>
</body>
</html>
```

---

## 📄 라이선스

MIT License
